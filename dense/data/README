Instructions to generate data files:

	1. load in julia:
		
		module load julia

	2. request GPU time:

		srun --pty --account=cis500 --gres=gpu:1 --mem=8G --time=0-00:30:00 --partition=testgpu bash

	3. start julia:

		julia matrixgenerator.jl

