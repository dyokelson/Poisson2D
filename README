Instructions for Talapas:

	1. load in cuda:
		
		module load cuda/9.2

	2. request GPU time:

		srun --pty --account=cis500 --gres=gpu:1 --mem=8G --time=0-00:30:00 --partition=testgpu bash

	3. compile program:

		nvcc main.cpp gpu_operations.cu -o compute -lcublas

	4. run program: 

		./compute
